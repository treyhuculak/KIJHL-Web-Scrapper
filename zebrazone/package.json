{
  "name": "zebrazone",
  "private": true,
  "version": "0.0.0",
  "description": "Runs the app locally: npm start for everything, npm run locally for just the two halves",
  "scripts": {
    "start": "npm run db && npm run locally",
    "locally": "concurrently -k -n api,web -c cyan,magenta \"cd backend && uvicorn app.main:app --reload --port 8100\" \"cd frontend && npm run dev\"",
    "db": "docker compose up -d --wait",
    "db:stop": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
